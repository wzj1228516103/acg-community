# 漫化 ACG 社区平台

## 项目概述

漫化 ACG 社区平台面向化妆师、假睫毛造型师及二次元文化爱好者，提供商品交易、化妆服务预约、用户交流和运营管理能力。平台采用前后端分离架构，覆盖普通用户、化妆师、商家和管理员等角色的主要业务流程。

## 主要功能

- 用户注册、登录、个人资料维护，以及基于角色的访问控制。
- 商品商城：商品分类、浏览、详情查看、购物车、订单创建和订单查询。
- 化妆服务：服务发布、可预约时段管理、预约创建和预约状态管理。
- 商家与化妆师入驻申请及后台审核。
- 收藏、站内聊天和文件上传。
- 管理后台：用户、商品、分类、订单、化妆服务和申请记录管理。

## 技术栈

| 分层 | 技术 |
| --- | --- |
| 前端 | Vue 3、Vite、Vue Router、Pinia、Element Plus、Axios、ECharts |
| 后端 | Java 17、Spring Boot 3.2、MyBatis-Plus、Sa-Token、WebSocket |
| 数据与缓存 | MySQL 8、Redis |
| 构建工具 | Maven、npm |

## 项目结构

```text
acg-community/
├── acg-frontend/                 # Vue 3 前端工程
│   ├── src/api/                  # 后端接口封装
│   ├── src/views/                # 商城、预约、订单、聊天和后台页面
│   ├── src/stores/               # Pinia 状态管理
│   └── src/router/               # 路由与权限控制
├── acg-backend/                  # Spring Boot 后端工程
│   ├── src/main/java/            # 控制器、服务、实体、数据访问层
│   └── src/main/resources/       # 应用配置与数据库初始化脚本
└── 需求文档/                      # 项目需求说明
```

## 本地运行

### 前置条件

- JDK 17
- Maven 3.9 或更高版本
- Node.js 18 或更高版本
- MySQL 8
- Redis

### 初始化数据库

执行 `acg-backend/src/main/resources/db/schema.sql` 中的建表脚本，并根据本地环境修改 `acg-backend/src/main/resources/application.yml` 的数据库、Redis 和文件访问配置。

### 启动后端

```powershell
cd acg-backend
mvn spring-boot:run
```

后端默认运行在 `http://localhost:8081/api`。

### 启动前端

```powershell
cd acg-frontend
npm install
npm run dev
```

前端开发服务器默认运行在 `http://localhost:5173`，并将 `/api` 请求代理至后端。

## 说明

生产部署前应使用环境变量或外部配置管理数据库密码、密钥和第三方服务地址，避免将敏感信息提交到版本库。
